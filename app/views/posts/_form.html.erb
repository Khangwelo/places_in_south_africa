<%= form_for @post do |f| %>
	<% if @post.errors.any? %>
	  <div id="error_explanation">
	    <h2>
	      <%= pluralize(@post.errors.count, "error") %> prohibited
	      this article from being saved:
	    </h2>
	    <ul>
	      <% @post.errors.full_messages.each do |msg| %>
	        <li><%= msg %></li>
	      <% end %>
	    </ul>
	  </div>
	<% end %>
		
	 <%= f.label :bru_name%>
	 <%= f.text_field :bru_name%><br>

	 <%= f.label :title %>
	 <%= f.text_field :title %><br>

	 <%= f.label :body %>
	 <%= f.text_area :body %><br>

	 <%= f.submit %>
<% end %><!-- Fill in code here to create a form that can be used for either creating or editing a post (see Rails Guides Getting Started section 5.12 if you need help) -->

<!-- BONUS! Add some error handling -->